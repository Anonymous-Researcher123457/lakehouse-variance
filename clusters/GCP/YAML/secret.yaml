apiVersion: v1
kind: Secret
metadata:
  name: gcs-sa-key
type: Opaque
data:
  SA_keyfile.json: |
    <BASE64_ENCODED_GCP_SERVICE_ACCOUNT_KEY>   # REQUIRED: replace with base64-encoded contents of your GCP service account key JSON